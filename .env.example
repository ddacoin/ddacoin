# DDACOIN node – copy to .env and adjust
# RPC credentials (explorer must use the same)
RPC_USER=ddacoin
RPC_PASS=ddacoin
# Mining payout address (required when using --generate). Must be a DDACOIN address (starts with D).
# Get one: create a wallet in the web wallet and use its receive address, or run: go run ./cmd/genaddress
MINING_ADDR=
