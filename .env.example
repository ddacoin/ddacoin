# DDACOIN node – copy to .env and adjust
# RPC credentials (explorer must use the same)
RPC_USER=ddacoin
RPC_PASS=ddacoin
# Mining payout address (required when using --generate). Must be a DDACOIN address (starts with D).
# Get one: create a wallet in the web wallet and use its receive address, or run: go run ./cmd/genaddress
MINING_ADDR=
# Optional: enable high-power defaults (more peers/caches)
# Set to any non-empty value to enable, leave empty to disable.
HIGHPOWER=

# Optional: disable outbound IPv6 connections (skip IPv6 peers).
# Useful if your network/ISP does not support IPv6.
# Set to any non-empty value to enable, leave empty to disable.
NO_IPV6=

# Optional: your node's external/public IP.
# Prevents self-connection loops when peers advertise your IP.
EXTERNAL_IP=

# Optional: run DDACOIN testnet.
# Default is mainnet when this is empty or unset.
# Set to any non-empty value to add --ddacointestnet.
DDACOIN_TESTNET=
