# DDACOIN Wallet – copy to .env and adjust
# Node RPC (must match node's --rpcuser/--rpcpass). Node uses TLS by default; wallet uses HTTPS and skips cert verify.
# Active wallet network: mainnet | testnet (default: mainnet)
DDACOIN_NETWORK=mainnet
# Docker network shared with the node compose stack.
# mainnet: ddacoin-net
# testnet: ddacoin-testnet-net
NODE_DOCKER_NETWORK=ddacoin-net
# RPC_HOST defaults by network if unset (mainnet: ddacoin-node, testnet: ddacoin-testnet-node)
RPC_HOST=
# RPC_PORT defaults by network if unset (mainnet: 9667, testnet: 19667)
RPC_PORT=
RPC_USER=ddacoin
RPC_PASS=ddacoin
# If node runs on host: RPC_HOST=host.docker.internal (or 172.17.0.1 on Linux). Ensure wallet is on node network when using ddacoin-node.
# For testnet by container name, use:
# DDACOIN_NETWORK=testnet
# NODE_DOCKER_NETWORK=ddacoin-testnet-net
# RPC_HOST=ddacoin-testnet-node
# Port the wallet UI/API listens on (host side)
WALLET_PORT=3080
