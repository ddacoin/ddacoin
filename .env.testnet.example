# DDACOIN testnet node – copy to .env.testnet and adjust
# Use with:
# docker compose --project-name ddacoin-testnet --env-file .env.testnet -f docker-compose.testnet.yml up -d

RPC_USER=ddacoin_testnet
RPC_PASS=ddacoin_testnet

# Testnet mining payout address (generate with: go run ./cmd/genaddress --ddacointestnet)
MINING_ADDR=

# Optional: enable high-power defaults (more peers/caches)
HIGHPOWER=

# Optional: disable outbound IPv6 connections
NO_IPV6=
