# DDACOIN Explorer – copy to .env and adjust
# Node RPC (must match node's --rpcuser/--rpcpass)
RPC_HOST=host.docker.internal
RPC_PORT=9667
RPC_USER=ddacoin
RPC_PASS=ddacoin
# Optional: override with a single URL including auth
# RPC_URL=http://user:pass@host.docker.internal:9667
# Port the explorer listens on (host side)
EXPLORER_PORT=9080
