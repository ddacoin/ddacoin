# DDACOIN Explorer – copy to .env and adjust
# Node RPC (must match node's --rpcuser/--rpcpass).
# Active explorer network: mainnet | testnet (default: mainnet)
DDACOIN_NETWORK=mainnet
# Explorer container name (set ddacoin-testnet-explorer for testnet to avoid conflicts)
EXPLORER_CONTAINER_NAME=ddacoin-explorer
# Docker network shared with node stack.
# mainnet: ddacoin-net
# testnet: ddacoin-testnet-net
NODE_DOCKER_NETWORK=ddacoin-net
# RPC_HOST defaults by network if unset (mainnet: ddacoin-node, testnet: ddacoin-testnet-node)
# Otherwise (explorer on another host): set RPC_HOST to the node's IP/hostname.
RPC_HOST=
# RPC_PORT defaults by network if unset (mainnet: 9667, testnet: 19667)
RPC_PORT=
# For testnet by container name, use:
# DDACOIN_NETWORK=testnet
# EXPLORER_CONTAINER_NAME=ddacoin-testnet-explorer
# NODE_DOCKER_NETWORK=ddacoin-testnet-net
# RPC_HOST=ddacoin-testnet-node
RPC_USER=ddacoin
RPC_PASS=ddacoin
# Optional: full URL (overrides HOST/PORT). Node defaults to TLS, so use https://
# RPC_URL=https://ddacoin-node:9667
# Port the explorer listens on (host side)
EXPLORER_PORT=9080
