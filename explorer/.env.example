# DDACOIN Explorer – copy to .env and adjust
# Node RPC (must match node's --rpcuser/--rpcpass).
# When node and explorer share Docker network ddacoin-net (see ddacoin/ and explorer/ docker-compose):
#   RPC_HOST=ddacoin-node
# Otherwise (explorer on another host): set RPC_HOST to the node's IP/hostname.
RPC_HOST=ddacoin-node
RPC_PORT=9667
RPC_USER=ddacoin
RPC_PASS=ddacoin
# Optional: full URL (overrides HOST/PORT). Use http:// not https://
# RPC_URL=http://ddacoin:ddacoin@host.docker.internal:9667
# Port the explorer listens on (host side)
EXPLORER_PORT=9080
